# remove-duplicate-srt

Removes duplicate lines in a .srt file and keeps the enumeration and timestamps correct.

## Requirements

- [uv](https://docs.astral.sh/uv/)

## Quickstart

1. `uv run main.py --help`
